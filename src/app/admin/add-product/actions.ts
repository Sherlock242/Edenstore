
'use server';

// This file is intentionally left empty. 
// The addProduct function has been moved to src/app/actions.ts 
// to consolidate data management.
// We are keeping the file to avoid breaking existing imports, but it should be considered deprecated.

export type {} from '@/app/actions';
